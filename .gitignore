# Byte-compiled / cache
__pycache__/
*.py[cod]
*.so
.pytest_cache/
.ruff_cache/

# Virtualenvs
project_venv/
venv/
.venv/
env/

# Django
db.sqlite3
media/
staticfiles/
*.log

# Trained model artifacts. These are built by `python -m classifier.train`
# and by the Docker image at build time. Committing them is what left this
# repository shipping four scikit-learn 0.18 pickles that no currently
# installable version can load.
artifacts/

# Local README preview (generated by scripts/preview-readme.sh)
README.preview.html

.DS_Store
